
function signInValidate() {
    var semail=document.getElementById("SIGN_IN_MAIL").value;
    var spword=document.getElementById("SIGN_IN_PWORD").value;

    if (semail=="")
    {
        alert("E mail should not be empty!");
        return;
    }
    if(!semail.includes("@","."))
   {
    alert("Enter valid E mail Adress");
        return;
    }
     
    if (spword=="")
    {
        alert("Password should not be empty!");
        return;
    }
    
   

}

function signUpValidate() 
{    


     var name=document.getElementById("NME").value;
    var email=document.getElementById("MAIL").value;
     var PWord =document.getElementById("PWORD").value;
     var RWORD=document.getElementById("RWORD").value;




     

     if (email=="")
     {
         alert("E mail should not be empty!");
         return;
     } 

     if(!email.includes("@","."))
     {
         alert("Enter valid E mail Adress");
         return;
     }

     if (name=="")
     {
         alert("Name should not be empty!");
         return;
     }

    

     if (PWord=="")
     {
         alert("Password should not be empty!");
         return;
       
     }
     if(PWord.length!==6)
     {
        alert("password should be 6 charactors");
        return;
     }

     if(PWord!=RWORD)
     {
         alert("password do not match");
         return;
     }
     


     
     
     if (RWORD=="")
     {
         alert("Confirm password should not be empty!");
         return;
         
     }

    }

    
